# MCP2 대시보드

MCP 서버 설명의 **가시성**을 매일 관측한 결과. 경희대 SIFT Lab.

LLM 이 읽는 텍스트와 사람이 볼 수 있는 텍스트의 차이를 잽니다 —
JSON Schema 안쪽, UI 절단선 뒤, 마크다운 렌더링에 소멸하는 자리.

이 저장소에는 빌드된 정적 페이지와 `data.json` 만 있습니다.
수집기 코드는 비공개입니다.

- 색: **보라** 레지스트리 UI 에서 보이는 것 · **청록** 직접 찔러야 보이는 것 · **빨강** 사람 눈에 안 보이는 자리
- `data.json` 은 매일 02:00 KST 수집 직후 갱신됩니다
